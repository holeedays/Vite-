const Footer = () => {

    return (
        <div>
            <h3> Okay, now we exit </h3>
        </div>
    );
}

// NOTE: When exporting constants, you need to put the export declaration at the bottom
export default Footer
